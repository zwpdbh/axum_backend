[package]
name = "axum_graphql"
version = "0.1.0"
edition = "2021"

[dependencies]
seaorm_demo = { path = "../seaorm_demo" }
tokio = { version = "1.32.0", features = ["full"] }
dotenv = "0.15.0"
clap = { version = "4.5", features = ["derive"] }
tracer = { path = "../tracer" }
